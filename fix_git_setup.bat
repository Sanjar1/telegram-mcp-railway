@echo off echo Fixing Git Configuration... echo. echo Step 1: Setting up Git identity... git config --global user.email "sanjar@example.com" git config --global user.name "Sanjar1" echo. echo Step 2: Removing wrong remote... git remote remove origin echo. echo Step 3: Adding correct remote... git remote add origin https://github.com/Sanjar1/telegram-mcp-railway.git echo. echo Step 4: Verifying remote... git remote -v echo. echo Step 5: Pushing to YOUR repository... git push -u origin main echo. echo Done! Check if push was successful. pause
